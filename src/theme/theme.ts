import { baseColors } from './style/constants'

const theme = {
  bg: `${baseColors.bgPrimaryClr}`,
  textColor: `${baseColors.textPrimaryClr}`,
  bgContent: `${baseColors.bgPrimaryContentClr}`,
}

export default theme
